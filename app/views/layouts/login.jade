doctype html
html
	include ../includes/head

	block vars

	- if (subText === undefined)
		- subText = 'Oops'

	body
		include ../includes/header

		div(class="l-container")

			.page-content
				form.form--login
					label
						| Let’s get started…
						input.btn.control--submit(type='submit' value="Sign in with Twitter")
					input(id='requestToken' class='control-submit' value=requestToken type='hidden')




				.loading.isHidden

				video.cameraWindow.isHidden
				canvas.photoWindow.isHidden
				img.photo.isHidden

			include ../includes/footer

	include ../includes/foot